{% extends 'base.html.twig' %}

{% block title %}Edit {{ destination.name }} - Admin{% endblock %}

{% block body %}
    <h1>Edit: {{ destination.name }}</h1>
    <a class="btn btn-secondary" href="{{ path('app_admin_destination_index') }}">&larr; Back to list</a>

    <div class="card" style="margin-top: 20px;">
        {{ form_start(form) }}
            {% for field in form %}
                {% if not field.rendered %}
                    <div>
                        {{ form_label(field) }}
                        {{ form_widget(field) }}
                        {{ form_errors(field) }}
                    </div>
                {% endif %}
            {% endfor %}
            <button type="submit">Save changes</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
